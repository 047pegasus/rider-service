# from apps.riders.views import RiderLocationsViewSet, RiderViewSet
# from rest_framework.routers import DefaultRouter

# from apps.orders.views import OrderViewSet
# from apps.deliveries.views import DeliveryViewSet, BatchDeliveryViewSet
# from apps.events.views import DeliveryEventViewSet
# from apps.notifications.views import NotificationViewSet

# Create main router
# router = DefaultRouter()

# Register viewsets
# router.register(r"riders", RiderViewSet, basename="rider")
# router.register(r"rider-locations", RiderLocationsViewSet, basename="rider-location")
# router.register(r'orders', OrderViewSet, basename='order')
# router.register(r'deliveries', DeliveryViewSet, basename='delivery')
# router.register(r'batch-deliveries', BatchDeliveryViewSet, basename='batch-delivery')
# router.register(r'events', DeliveryEventViewSet, basename='delivery-event')
# router.register(r'notifications', NotificationViewSet, basename='notification')
